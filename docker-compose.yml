services:
  web:
    build: .
    env_file:
      - gitlab-webhook-flask.env
    security_opt:
      - no-new-privileges:true
    ports:
      - 5000:5000
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 50M
